/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'hrstixyeexwwaamxmhnr.supabase.co',
      },
    ],
  },
}

export default nextConfig
